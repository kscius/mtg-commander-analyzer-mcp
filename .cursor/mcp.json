{
  "mcpServers": {
    "mtg-commander-analyzer": {
      "command": "node",
      "args": ["${workspaceFolder}/scripts/run-mcp.cjs"],
      "cwd": "${workspaceFolder}",
      "env": {
        "PATH": "C:\\nvm4w\\nodejs;${env:PATH}"
      }
    }
  }
}
