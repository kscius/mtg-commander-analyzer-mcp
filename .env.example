# Optional: copy to .env if you add project-specific env vars later.
# The MCP server does not require API keys — Cursor (or your agent) provides LLM reasoning.
# Card data lives in data/cards.db (npm run db:create && npm run db:import).
